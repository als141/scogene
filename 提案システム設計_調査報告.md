# モンジェネ 提案システム設計 — 大規模調査報告書

## 目次
1. [システム全体像](#1-システム全体像)
2. [学習サイクルの設計とロジック](#2-学習サイクルの設計とロジック)
3. [採点・苦手分析エンジン](#3-採点苦手分析エンジン)
4. [教師向けフィードバックシステム](#4-教師向けフィードバックシステム)
5. [個別最適化問題生成エンジン](#5-個別最適化問題生成エンジン)
6. [完全習得学習（マスタリーラーニング）の設計](#6-完全習得学習マスタリーラーニングの設計)
7. [データベース戦略 — 参入障壁としてのデータモート](#7-データベース戦略--参入障壁としてのデータモート)
8. [技術アーキテクチャ](#8-技術アーキテクチャ)
9. [学術的根拠・引用一覧](#9-学術的根拠引用一覧)

---

## 1. システム全体像

### コアループ：「採点→分析→生成→再採点」の無限サイクル

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│    ① 問題を解く          ② 採点・苦手分析                      │
│    ┌──────────┐          ┌──────────────┐                      │
│    │ 生徒が   │ ──手書き→│ AI採点エンジン│                      │
│    │ 問題を解く│  答案提出 │ (GPT-5.2 +   │                      │
│    └──────────┘          │  Code Interp) │                      │
│         ▲                └──────┬───────┘                      │
│         │                       │                               │
│         │                       ├── 正誤判定                    │
│         │                       ├── 間違いステップ特定          │
│         │                       ├── エラー分類                  │
│         │                       │   (計算ミス/公式忘れ/概念誤解) │
│         │                       └── 苦手プロファイル更新        │
│         │                               │                       │
│         │                               ▼                       │
│         │                 ③ フィードバック分岐                  │
│         │                ┌────────┴────────┐                    │
│         │                ▼                 ▼                    │
│         │   【教師向け】             【生徒向け】               │
│         │   生徒別レポート           対策問題生成               │
│         │   ・苦手ランキング         ・苦手克服問題             │
│         │   ・エラーパターン分析     ・難易度調整済み           │
│         │   ・推奨指導ポイント       ・段階的ステップアップ     │
│         │                                 │                     │
│         │                                 ▼                     │
│         │                       ④ 個別最適化問題               │
│         │                       ┌──────────────┐               │
│         └───────────────────────│ 生徒が解く   │               │
│                                 └──────────────┘               │
│                                                                 │
│              ⑤ マスタリー判定: 85%以上 → 次の単元へ進む       │
│                              85%未満 → サイクル継続            │
└─────────────────────────────────────────────────────────────────┘
```

**このサイクルの学術的根拠：**

Benjamin Bloomの「2シグマ問題」(1984) によれば、1対1の個別指導+完全習得学習を組み合わせると、従来の集団授業と比べて**2標準偏差（偏差値で20相当）**の学力向上が見られた。つまり、平均的な生徒が上位2%の成績に到達する。Kulik et al. (1990) の103研究のメタ分析では、完全習得学習単体でも**効果量 d = 0.52**が確認されている。

モンジェネは、このサイクルをAIで自動化することで、**1対1個別指導の効果を、1対多の塾環境で再現する**ことを目指す。

---

## 2. 学習サイクルの設計とロジック

### 2.1 サイクルの各フェーズと所要時間

| フェーズ | 内容 | 所要時間 | 担当 |
|---------|------|---------|------|
| ① 問題提示 | 生徒のレベルに応じた問題を提示 | 即時（AI生成済み or DB検索） | システム |
| ② 解答 | 生徒が手書きで解く | 10〜30分（問題数による） | 生徒 |
| ③ 採点・分析 | 手書き答案をAIが採点 | 30秒〜2分 | AI |
| ④ FB生成 | 教師向けレポート＋生徒向け対策問題 | 1〜3分 | AI |
| ⑤ マスタリー判定 | 習得度チェック、次のアクション決定 | 即時（自動判定） | システム |

### 2.2 エラー分類体系（Taxonomy of Mathematical Errors）

先行研究（Radatz, 1979; Newman, 1977）および塾現場の経験に基づき、以下の5層エラー分類を採用する：

```
Level 1: 読解エラー (Reading Error)
  └─ 問題文の読み間違い、条件の見落とし
  └─ 例：「AからBまでの距離」を「BからAまでの距離」と読み違え

Level 2: 理解エラー (Comprehension Error)
  └─ 問題の意味は読めるが、何を求めるべきか理解できていない
  └─ 例：面積を求める問題で体積を求めてしまう

Level 3: 変換エラー (Transformation Error)
  └─ 問題を数式に変換する段階でのミス
  └─ 例：「3倍より2大きい」を 3x - 2 とする（正しくは 3x + 2）

Level 4: 処理エラー (Process Skill Error)
  └─ 正しい方針だが計算過程でミス
  └─ 例：移項時の符号ミス、かけ算の計算ミス
  └─ サブ分類：
      ├─ 符号ミス (Sign Error)
      ├─ 四則演算ミス (Arithmetic Error)
      ├─ 公式の記憶違い (Formula Recall Error)
      └─ 約分・通分ミス (Fraction Operation Error)

Level 5: 書き写しエラー (Encoding Error)
  └─ 解法は正しいが最終解答の書き写しミス
  └─ 例：途中で正しい値が出ているのに最後に転記ミス
```

### 2.3 苦手プロファイルのデータ構造

```typescript
interface StudentWeaknessProfile {
  student_id: string;

  // 単元レベルの苦手度（0.0〜1.0、高いほど苦手）
  unit_weakness: {
    [unit: string]: {  // "二次方程式", "空間図形", etc.
      weakness_score: number;      // 加重移動平均
      error_count: number;         // 総エラー数
      attempt_count: number;       // 総解答数
      last_attempted: Date;
      mastery_status: "not_started" | "learning" | "mastered" | "review_needed";
    }
  };

  // エラーパターン別の頻度
  error_patterns: {
    [error_type: string]: {   // "sign_error", "formula_recall", etc.
      frequency: number;       // 直近20問での発生率
      trend: "improving" | "stable" | "worsening";
      related_units: string[]; // このエラーが多発する単元
    }
  };

  // 概念レベルの習得状況（Knowledge Tracing）
  concept_mastery: {
    [concept: string]: {
      p_mastery: number;    // BKTによる習得確率 (0.0〜1.0)
      stability: number;    // FSRSの安定度（高い=忘れにくい）
      retrievability: number; // FSRSの想起確率
      next_review: Date;    // 最適な復習タイミング
    }
  };

  // 認知特性
  cognitive_profile: {
    preferred_difficulty: number;       // IRT推定の能力パラメータ θ
    processing_speed: "fast" | "average" | "slow";
    needs_visual_aid: boolean;         // 図形的説明が有効か
    preferred_explanation_depth: "brief" | "standard" | "detailed";
  };
}
```

---

## 3. 採点・苦手分析エンジン

### 3.1 現在のプロトタイプ（実装済み）

既存の `backend/agent.py` では、以下が実装済み：
- GPT-5.2 + Code Interpreter による手書き答案の採点
- 日本式の採点記号（○×△）の画像アノテーション
- 問題ごとの正誤判定、途中式評価、部分点理由、改善ヒント
- 構造化出力 `GradingResult` によるJSON形式の採点結果

### 3.2 苦手分析の拡張設計

現在の単発採点を、**蓄積型の苦手分析**に発展させる：

```
[単発採点結果]                    [蓄積された学習履歴]
GradingResult                     StudentWeaknessProfile
  │                                      │
  ├── questions[].is_correct             │
  ├── questions[].process_evaluation ────┤── error_patterns の更新
  ├── questions[].improvement_hint       │
  ├── weaknesses[]                       │── unit_weakness の更新
  └── study_advice[]                     │
                                         │
                          ┌──────────────┤
                          ▼              ▼
              Bayesian Knowledge     FSRS v6
              Tracing (BKT)          間隔反復スケジューラ
                          │              │
                          ▼              ▼
              concept_mastery        next_review
              p_mastery 更新         復習日の計算
```

#### BKT (Bayesian Knowledge Tracing) の適用

各「概念（concept）」に対して、以下の4パラメータで習得状態を追跡する：

- **P(L₀)**: 初期習得確率（デフォルト 0.1）
- **P(T)**: 学習遷移確率（1回の練習で習得する確率、概念ごとに異なる）
- **P(G)**: 当て推量確率（未習得なのに正答する確率、デフォルト 0.25）
- **P(S)**: うっかりミス確率（習得済みなのに誤答する確率、デフォルト 0.1）

**更新式：**
```
正答時: P(Lₙ) = P(Lₙ₋₁)(1-P(S)) / [P(Lₙ₋₁)(1-P(S)) + (1-P(Lₙ₋₁))P(G)]
誤答時: P(Lₙ) = P(Lₙ₋₁)P(S) / [P(Lₙ₋₁)P(S) + (1-P(Lₙ₋₁))(1-P(G))]
次回予測: P(Lₙ₊₁) = P(Lₙ) + (1-P(Lₙ))P(T)
```

**習得判定閾値：** P(L) ≥ 0.85 → 「習得」と判定

**根拠：** Corbett & Anderson (1994) によるBKTは、Carnegie Learning / Cognitive Tutorの基盤技術として20年以上の実績がある。効果量 d = 0.35〜0.57（Kulik & Fletcher, 2016 メタ分析）。

#### FSRS v6 による復習スケジューリング

BKTが「何を理解しているか」を追跡するのに対し、FSRS (Free Spaced Repetition Scheduler) は「いつ復習すべきか」を最適化する。

FSRS v6の3つのコア変数：
- **Difficulty (D)**: 問題固有の難しさ（0〜10）
- **Stability (S)**: 記憶の安定度（高い→忘却が遅い）
- **Retrievability (R)**: 想起確率（時間とともに減衰）

SM-2（Anki等）と比較して**20〜30%少ない復習回数**で同等の定着率を実現（Ye et al., 2024; LECTOR benchmark: FSRS 89.6% vs SM-2 lower）。

オープンソース実装が Python / TypeScript / Rust で利用可能：https://github.com/open-spaced-repetition/free-spaced-repetition-scheduler

---

## 4. 教師向けフィードバックシステム

### 4.1 生徒別レポートの設計

塾講師が求めるのは「次の授業で何を教えるべきか」の判断材料である。

```
╔══════════════════════════════════════════════════════════════╗
║  生徒レポート: 田中太郎（中3・志望校: 新潟高校）            ║
║  期間: 2026/02/20 ~ 2026/02/27                              ║
╠══════════════════════════════════════════════════════════════╣
║                                                              ║
║  ■ 総合成績                                                  ║
║  ┌──────────────────────────────────────────────┐            ║
║  │ 今週の正答率: 62% (前週比 +8%)               │            ║
║  │ 解いた問題数: 24問                            │            ║
║  │ 習得した概念: 3 / 未習得: 5                   │            ║
║  └──────────────────────────────────────────────┘            ║
║                                                              ║
║  ■ 苦手ランキング（優先対応順）                              ║
║  ┌──┬──────────────┬──────────┬────────────────────┐        ║
║  │1 │ 空間図形の体積│ 正答率23%│ 切断面の把握ができて ║        ║
║  │  │              │          │ いない。展開図から   ║        ║
║  │  │              │          │ 復習が必要           ║        ║
║  ├──┼──────────────┼──────────┼────────────────────┤        ║
║  │2 │ 二次方程式   │ 正答率45%│ 因数分解はできるが、 ║        ║
║  │  │              │          │ 解の公式を使う判断が ║        ║
║  │  │              │          │ 弱い                 ║        ║
║  ├──┼──────────────┼──────────┼────────────────────┤        ║
║  │3 │ 符号の処理   │ 誤答の40%│ 移項時の符号ミスが   ║        ║
║  │  │              │          │ 全エラーの4割を占める ║        ║
║  └──┴──────────────┴──────────┴────────────────────┘        ║
║                                                              ║
║  ■ 推奨指導アクション                                        ║
║  1. 空間図形: 展開図→見取図の対応を実物模型で確認させる      ║
║  2. 二次方程式: 判別式D>0,=0,<0の場合分けを再指導             ║
║  3. 符号ミス: 途中式を1行ずつ確認する習慣づけ                 ║
║                                                              ║
║  ■ エラーパターン推移（直近4週間）                            ║
║  計算ミス:    ████████░░ 80% → ██████░░░░ 60%  ↓改善中      ║
║  公式忘れ:    ████░░░░░░ 40% → ████░░░░░░ 40%  →横ばい      ║
║  概念誤解:    ██░░░░░░░░ 20% → ████░░░░░░ 40%  ↑要注意      ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝
```

### 4.2 クラス全体ビュー

```
╔══════════════════════════════════════════════════════╗
║  クラス概況: 中3Aクラス（15名）                      ║
╠══════════════════════════════════════════════════════╣
║                                                      ║
║  ■ 単元別・クラス平均習得率                          ║
║  一次関数     ████████████████████ 95% ✓完了         ║
║  連立方程式   ████████████████░░░░ 80% ✓完了         ║
║  二次方程式   ████████████░░░░░░░░ 60% ⚠進行中      ║
║  相似・合同   ████████░░░░░░░░░░░░ 45% ⚠進行中      ║
║  空間図形     ████░░░░░░░░░░░░░░░░ 25% ✗要強化      ║
║                                                      ║
║  ■ 要注意生徒（習得率が平均-1σ以下）                ║
║  ・佐藤花子: 二次方程式 30%（クラス平均60%）         ║
║  ・鈴木一郎: 空間図形 10%（クラス平均25%）           ║
║                                                      ║
║  ■ 次回授業の推奨テーマ                              ║
║  「二次方程式の解の公式」（クラスの65%が未習得）     ║
╚══════════════════════════════════════════════════════╝
```

### 4.3 フィードバック生成のロジック

```python
def generate_teacher_feedback(student: StudentWeaknessProfile) -> TeacherReport:
    # 1. 苦手を重み付きでランキング
    #    重み = weakness_score × recency_factor × importance_for_exam
    ranked_weaknesses = rank_weaknesses(
        student.unit_weakness,
        exam_weight=get_exam_weight(student.target_school),  # 志望校の出題傾向
        recency_decay=0.9  # 直近のデータほど重視
    )

    # 2. エラーパターンのトレンド分析
    error_trends = analyze_trends(student.error_patterns, window=4)  # 直近4週間

    # 3. LLMによる指導アドバイス生成
    #    入力: 苦手ランキング + エラートレンド + 志望校情報
    #    出力: 具体的な指導アクション（自然言語）
    advice = llm_generate_advice(ranked_weaknesses, error_trends, student.target_school)

    # 4. 次回推奨問題セットの自動構成
    recommended_problems = select_problems(
        weaknesses=ranked_weaknesses[:3],  # 上位3苦手に集中
        difficulty=student.cognitive_profile.preferred_difficulty,
        count=5
    )

    return TeacherReport(
        student_id=student.student_id,
        weakness_ranking=ranked_weaknesses,
        error_trends=error_trends,
        teaching_advice=advice,
        recommended_problems=recommended_problems
    )
```

---

## 5. 個別最適化問題生成エンジン

### 5.1 問題生成の3モード

既存プロトタイプの「A/B/C 3パターン」を発展させ、苦手プロファイルに基づく生成に拡張する：

| モード | 目的 | 入力 | 出力 |
|-------|------|------|------|
| **苦手克服モード** | 特定の苦手を集中的に克服 | 苦手プロファイル + エラーパターン | その苦手にピンポイントの問題 |
| **段階的ステップアップモード** | 前提知識から段階的に難易度を上げる | 現在の習得状況 + 知識グラフ | 前提→基礎→応用の問題セット |
| **模試対策モード** | 志望校の出題傾向に合わせた問題 | 志望校 + 現在の学力 + DB | 本番形式の問題セット |

### 5.2 苦手克服問題生成のロジック

```
Input:
  - weakness: "空間図形の切断面の把握"
  - error_type: "transformation_error" (立体→平面への変換ができない)
  - current_difficulty: 2 (標準)
  - mastery_probability: 0.35

Generation Strategy:
  1. 知識グラフから前提概念を確認
     └─ "空間図形の切断面" の前提:
         ├─ "展開図の理解" (P(L) = 0.70 → OK)
         ├─ "断面の形状判定" (P(L) = 0.30 → ★ここが弱い)
         └─ "三平方の定理" (P(L) = 0.85 → OK)

  2. 最も弱い前提概念にフォーカス
     → "断面の形状判定" を強化する問題を生成

  3. 難易度を現在のレベルより少し下から開始
     → difficulty = max(1, current_difficulty - 1) = 1

  4. LLMに以下のコンテキストで問題生成を依頼：
     - 単元: 空間図形
     - フォーカス概念: 断面の形状判定
     - 難易度: 1 (基礎)
     - 避けるべき要素: 三平方の定理の複雑な計算（これは既習得）
     - 生徒のエラー傾向: 立体から平面への射影が苦手
     - 参考問題: DBから類似問題を3問検索して Few-shot として提供
```

### 5.3 問題選択アルゴリズム：Thompson Sampling

DBに蓄積された問題から最適な問題を選ぶ際、**Thompson Sampling（トンプソンサンプリング）**を採用する。

**なぜThompson Samplingか：**
- Multi-Armed Bandit問題の最適解として知られ、regret（後悔値）が最小（26.8 vs epsilon-greedyの192.3、10腕・1000回の実験結果）
- 探索（新しい問題を試す）と活用（効果が高いと分かっている問題を出す）のバランスが自動的に最適化される
- 実装がシンプルで、リアルタイム適応が可能

**アルゴリズム：**
```python
def select_next_problem(student: StudentWeaknessProfile, candidates: list[Problem]) -> Problem:
    """Thompson Samplingで次の問題を選択"""
    best_score = -float('inf')
    best_problem = None

    for problem in candidates:
        # 各問題の「学習効果」をBeta分布からサンプリング
        # alpha = この問題で正答→習得に進んだ回数 + 1
        # beta = この問題で正答したが習得に進まなかった回数 + 1
        alpha = problem.success_transitions + 1
        beta = problem.failure_transitions + 1
        sampled_effectiveness = np.random.beta(alpha, beta)

        # 生徒との適合度を掛け合わせる
        relevance = compute_relevance(student.unit_weakness, problem.knowledge_components)
        difficulty_match = 1.0 - abs(student.cognitive_profile.preferred_difficulty - problem.difficulty_irt) / 5.0

        score = sampled_effectiveness * relevance * difficulty_match

        if score > best_score:
            best_score = score
            best_problem = problem

    return best_problem
```

**根拠：** IEEE HNICEM 2024の研究で、Thompson SamplingはAdaptive epsilon-greedy法と比較して、10腕で約7.2倍、20腕で約3.5倍少ないregretを達成。教育コンテンツ推薦においても最も効率的なアルゴリズムとして推奨されている。

### 5.4 DB検索 vs LLM新規生成の判断ロジック

```
問題が必要
  │
  ├─ DBに適合する問題があるか？（ハイブリッド検索）
  │   ├─ メタデータフィルタ: 単元、難易度、概念タグ
  │   └─ ベクトル類似度: 埋め込みベクトルでセマンティック検索
  │
  ├─[適合度 ≥ 0.85] → DB問題を使用（IRT較正済みで信頼性が高い）
  │
  ├─[0.50 ≤ 適合度 < 0.85] → DB問題をベースにLLMが数値・文脈を変更
  │
  └─[適合度 < 0.50] → LLMが新規生成
      │
      ├─ 生成後、数学的検証（SymPy + Code Execution）
      ├─ 逆検証（LLMが解き直して一致確認）
      └─ DB登録（未較正フラグ付き、生徒の回答データが集まったらIRT較正）
```

---

## 6. 完全習得学習（マスタリーラーニング）の設計

### 6.1 理論的背景

Bloomの完全習得学習（1968）の中核原理：
1. 学習目標を明確に定義する
2. 目標達成の基準（マスタリー基準）を設定する（通常80〜90%）
3. 基準に達しない生徒には**矯正的指導（corrective instruction）**を提供する
4. 基準を達成してから次の単元に進む

**効果量のエビデンス：**

| 研究 | 効果量 (Cohen's d) | サンプル |
|-----|-------------------|---------|
| Bloom (1984) 完全習得学習+個別指導 | **2.00** | 小規模 |
| Bloom (1984) 完全習得学習のみ | **1.00** | 小規模 |
| Kulik, Kulik & Bangert-Drowns (1990) メタ分析 | **0.52** | 103研究 |
| Kulik & Fletcher (2016) ITS メタ分析 | **0.66（中央値）** | 50研究 |
| Ma et al. ITS メタ分析 | **0.42** | 107効果量, 14,321名 |

Nintilの系統的レビューによると、Bloomの元の2σは過大推定の可能性があるが、スケーラブルな実装でも**0.4〜0.8σ**の効果は現実的。

### 6.2 モンジェネにおけるマスタリーラーニングの実装

```
中3数学 カリキュラム
├── 式の展開と因数分解
│   ├── 式の展開 ──────────── mastery_threshold: 85%
│   ├── 因数分解（基礎）───── mastery_threshold: 85%
│   ├── 因数分解（応用）───── mastery_threshold: 80%
│   └── ✓ 単元テスト ──────── mastery_threshold: 80%
│
├── 平方根
│   ├── 平方根の意味 ──────── mastery_threshold: 90%（基礎中の基礎）
│   ├── 根号の計算 ─────────── mastery_threshold: 85%
│   └── ✓ 単元テスト
│
├── 二次方程式 ← 前提: 因数分解 + 平方根
│   ├── 因数分解による解法 ── mastery_threshold: 85%
│   ├── 平方完成 ──────────── mastery_threshold: 80%
│   ├── 解の公式 ──────────── mastery_threshold: 85%
│   ├── 判別式 ────────────── mastery_threshold: 80%
│   └── ✓ 単元テスト
│
└── ...
```

#### マスタリー判定の仕組み

```python
def check_mastery(student_id: str, concept: str) -> MasteryDecision:
    profile = get_student_profile(student_id)
    concept_data = profile.concept_mastery[concept]

    # 条件1: BKTの習得確率が閾値以上
    bkt_mastered = concept_data.p_mastery >= MASTERY_THRESHOLD  # 0.85

    # 条件2: 直近N問の正答率が閾値以上（Nは信頼性確保のため最低5問）
    recent_accuracy = get_recent_accuracy(student_id, concept, n=5)
    accuracy_mastered = recent_accuracy >= 0.80

    # 条件3: 連続正答（最低3問連続正答で確認）
    consecutive_correct = get_consecutive_correct(student_id, concept)
    streak_confirmed = consecutive_correct >= 3

    if bkt_mastered and accuracy_mastered and streak_confirmed:
        return MasteryDecision(
            status="mastered",
            action="advance_to_next",
            next_review=fsrs_schedule_review(concept_data),  # FSRS で復習日を計算
            message=f"✓ 「{concept}」を習得しました！次の概念に進みましょう。"
        )
    elif bkt_mastered and not streak_confirmed:
        return MasteryDecision(
            status="near_mastery",
            action="confirm_with_problems",
            problems=select_confirmation_problems(concept, difficulty="current", count=3),
            message=f"もう少し！あと{3 - consecutive_correct}問正解で習得です。"
        )
    else:
        # 矯正的指導: 苦手分析に基づく対策問題を生成
        weakness = analyze_weakness_for_concept(student_id, concept)
        return MasteryDecision(
            status="learning",
            action="corrective_instruction",
            problems=generate_corrective_problems(weakness),
            hint=generate_scaffolding_hint(weakness),
            message=f"「{concept}」の{weakness.focus_area}を重点的に練習しましょう。"
        )
```

### 6.3 知識グラフによる前提条件管理

```
知識グラフ (DAG: 有向非巡回グラフ)

正の数・負の数 ──→ 文字式 ──→ 一次方程式 ──→ 連立方程式
                    │                              │
                    ▼                              ▼
               比例・反比例 ──→ 一次関数 ──→ 二次関数
                                                   │
平方根 ──→ 二次方程式 ─────────────────────────────┘
  │              │
  ▼              ▼
三平方の定理 ──→ 空間図形

合同条件 ──→ 相似条件 ──→ 円周角の定理
                │
                ▼
          面積比・体積比 ──→ 空間図形
```

**実装：** Math Academy (Skycak, 2024) の手法を参考に、各エッジに**包含重み（encompassing weight）**を持たせる。例えば、「二次方程式→二次関数」のエッジの包含重みが0.7なら、二次方程式の知識の70%が二次関数の学習に必要ということ。

**前提未習得の検出と自動補修：**
```python
def check_prerequisites(student_id: str, target_concept: str) -> list[Remediation]:
    """目標概念の学習に必要な前提概念の習得状況をチェック"""
    prerequisites = knowledge_graph.get_prerequisites(target_concept)
    remediations = []

    for prereq, weight in prerequisites:
        mastery = get_mastery(student_id, prereq)
        if mastery.p_mastery < PREREQUISITE_THRESHOLD:  # 0.70
            remediations.append(Remediation(
                concept=prereq,
                current_mastery=mastery.p_mastery,
                required_mastery=PREREQUISITE_THRESHOLD,
                encompassing_weight=weight,
                priority=weight * (PREREQUISITE_THRESHOLD - mastery.p_mastery)
            ))

    # 優先度順にソート（包含重みが高く、習得度が低い前提を最優先）
    return sorted(remediations, key=lambda r: r.priority, reverse=True)
```

atama+ の「根本原因分析」と同様のアプローチ：生徒が空間図形でつまずいている場合、真の原因は「三平方の定理」の理解不足かもしれない。知識グラフを遡って根本原因を特定し、そこから段階的に積み上げる。

---

## 7. データベース戦略 — 参入障壁としてのデータモート

### 7.1 データモートとは何か

データモート（Data Moat）とは、蓄積したデータ自体が競合の参入障壁となる状態を指す。モンジェネにおけるデータモートは以下の3層で構成される：

```
┌─────────────────────────────────────────────────┐
│ 第3層: 学習効果データ（最も価値が高い）          │
│ ・どの問題がどの生徒にどれだけ効果があったか     │
│ ・エラーパターンと矯正指導の成功率               │
│ ・概念間の前提関係の実証的検証データ             │
│ → 使えば使うほど精度が上がる「フライホイール」   │
├─────────────────────────────────────────────────┤
│ 第2層: アノテーション済み問題データベース        │
│ ・塾講師が品質保証した問題+解説                  │
│ ・IRT較正済みの難易度・識別度パラメータ          │
│ ・概念タグ・公式タグ・誤答パターンタグ           │
│ ・埋め込みベクトル（セマンティック検索用）        │
│ → 競合が一朝一夕に構築できない                   │
├─────────────────────────────────────────────────┤
│ 第1層: 原データ（入手困難な生データ）            │
│ ・統一模試協会の大規模問題データセット           │
│ ・各都道府県の入試問題（正答率・配点付き）       │
│ ・塾の過去問蓄積（実際の生徒の手書き答案含む）   │
│ → 提携・契約がなければ入手不可能                 │
└─────────────────────────────────────────────────┘
```

### 7.2 問題データベースのスキーマ設計

```sql
-- ========================================
-- 問題テーブル（IRT較正対応）
-- ========================================
CREATE TABLE problems (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

    -- 問題内容
    question_text TEXT NOT NULL,          -- 問題文（LaTeX含む）
    answer_text TEXT NOT NULL,            -- 模範解答
    explanation_text TEXT,                -- 解説
    figures JSONB,                        -- 図形データ（SVG/画像URL）

    -- 出典情報
    source TEXT,                          -- "統一模試2024第3回", "新潟県公立入試2025"
    source_year INTEGER,
    source_problem_number TEXT,

    -- 分類タグ（LLM構造化出力 + 塾講師検証）
    grade INTEGER NOT NULL,              -- 学年 (1-9: 小1〜中3)
    unit TEXT NOT NULL,                  -- 単元 "二次方程式"
    concepts TEXT[] NOT NULL,            -- 概念タグ ["因数分解", "解の公式"]
    formulas_used TEXT[],                -- 使用公式 ["解の公式", "因数分解公式"]
    operations TEXT[],                   -- 必要演算 ["四則演算", "平方根"]
    bloom_level INTEGER,                 -- Bloom's Taxonomy (1-6)
    has_figure BOOLEAN DEFAULT FALSE,
    has_story BOOLEAN DEFAULT FALSE,     -- 文章題かどうか
    solution_steps INTEGER,              -- 解法ステップ数

    -- IRT パラメータ（生徒の回答データから較正）
    difficulty_irt FLOAT,                -- IRT難易度 b （較正後）
    discrimination_irt FLOAT,            -- IRT識別度 a （較正後）
    guessing_irt FLOAT DEFAULT 0.25,     -- IRT当て推量 c
    irt_calibrated BOOLEAN DEFAULT FALSE,
    irt_sample_size INTEGER DEFAULT 0,   -- 較正に使った回答数

    -- 難易度（多元的）
    difficulty_level INTEGER,            -- 主観的難易度 1-5
    accuracy_rate FLOAT,                 -- 実測正答率（模試データから）
    accuracy_sample_size INTEGER,        -- 正答率の標本数
    cognitive_load INTEGER,              -- 認知負荷 1-5

    -- 埋め込みベクトル
    embedding VECTOR(768),               -- Gemini embedding / OpenAI embedding

    -- メタデータ
    verified_by TEXT,                     -- 検証した塾講師名
    verification_status TEXT DEFAULT 'unverified',  -- unverified / auto_verified / human_verified
    generation_method TEXT,              -- "human", "llm_generated", "llm_modified"
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
);

-- ========================================
-- 誤答パターンテーブル（問題ごとの典型的間違い）
-- ========================================
CREATE TABLE misconception_patterns (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    problem_id UUID REFERENCES problems(id),

    misconception_type TEXT NOT NULL,     -- "sign_error", "formula_recall", etc.
    description TEXT NOT NULL,            -- "移項時に符号を変え忘れる"
    incorrect_answer TEXT,               -- 典型的な誤答 "-3" (正答が3の場合)
    frequency FLOAT,                     -- この誤答パターンの出現率
    remediation_concept TEXT,            -- 矯正に必要な概念
    remediation_problems UUID[],         -- 矯正用の推奨問題ID

    -- アノテーション情報
    annotated_by TEXT,                   -- "llm" or 塾講師名
    verified BOOLEAN DEFAULT FALSE,

    created_at TIMESTAMPTZ DEFAULT now()
);

-- ========================================
-- 生徒の回答ログ（全インタラクション記録）
-- ========================================
CREATE TABLE student_interactions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    student_id UUID NOT NULL,
    problem_id UUID REFERENCES problems(id),
    session_id UUID,                     -- 学習セッション

    -- 回答データ
    is_correct BOOLEAN NOT NULL,
    student_answer TEXT,                 -- 生徒の解答
    response_time_ms INTEGER,            -- 回答にかかった時間
    hints_used INTEGER DEFAULT 0,        -- ヒント使用回数
    attempt_number INTEGER DEFAULT 1,    -- 同じ問題への試行回数

    -- AIによる分析結果
    error_type TEXT,                     -- エラー分類
    error_step INTEGER,                  -- 間違えたステップ番号
    process_evaluation TEXT,             -- 途中式の評価

    -- コンテキスト
    mode TEXT,                           -- "weakness_drill", "mastery_test", "exam_prep"
    difficulty_at_time FLOAT,            -- この時点でのIRT推定能力値 θ

    created_at TIMESTAMPTZ DEFAULT now()
);

-- ========================================
-- 知識グラフ（概念間の前提関係）
-- ========================================
CREATE TABLE knowledge_graph_edges (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    prerequisite_concept TEXT NOT NULL,   -- 前提概念
    target_concept TEXT NOT NULL,         -- 目標概念
    encompassing_weight FLOAT NOT NULL,  -- 包含重み (0.0-1.0)
    grade INTEGER,                       -- 学年
    verified BOOLEAN DEFAULT FALSE,      -- 塾講師による検証済みか
    evidence_source TEXT,                -- "curriculum_standard" / "student_data" / "expert"

    UNIQUE(prerequisite_concept, target_concept)
);

-- ========================================
-- 生徒プロファイル
-- ========================================
CREATE TABLE student_profiles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

    -- 基本情報
    name TEXT,
    grade INTEGER NOT NULL,
    target_school TEXT,                  -- 志望校
    cram_school_id UUID,                -- 所属塾

    -- 知識状態（JSONBで柔軟に管理）
    knowledge_state JSONB,              -- BKT/FSRS のパラメータ群
    weakness_profile JSONB,             -- StudentWeaknessProfile
    irt_ability FLOAT,                  -- IRT推定能力値 θ

    -- 埋め込み（生徒のクラスタリング用）
    student_embedding VECTOR(256),      -- 回答パターンの埋め込み

    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
);
```

### 7.3 塾講師によるアノテーション戦略

**目的：** LLMの構造化出力を「実際の塾講師の判断」と一致させる

#### Phase 1: アノテーションガイドライン作成

```
各問題に対して、塾講師は以下をアノテーション：

1. 難易度 (1-5)
   1: 基礎（教科書の例題レベル）
   2: 標準（教科書の章末問題レベル）
   3: やや難（公立高校入試の中盤レベル）
   4: 難（公立高校入試の最終問題レベル）
   5: 発展（私立難関校レベル）

2. 使用する公式・考え方
   - チェックリスト形式で選択
   - 例: □解の公式 □因数分解 □平方完成 □判別式 □代入法 ...

3. 前提知識
   - この問題を解くために事前に理解しておくべき概念
   - 知識グラフのエッジ情報として活用

4. 典型的な誤答パターン
   - 「この問題を間違える生徒はどう間違えるか」を記述
   - 各誤答パターンに対する矯正用解説

5. どんな生徒に出すべきか
   - 「因数分解はできるが解の公式の使い分けができない生徒」
   - → この自然言語記述を埋め込みベクトル化してマッチングに活用
```

#### Phase 2: LLM自動アノテーション + 人間検証

```python
def auto_annotate_problem(problem: Problem) -> Annotation:
    """LLMの構造化出力でアノテーション → 塾講師がレビュー"""

    # Step 1: LLMによる自動アノテーション
    annotation = llm_structured_output(
        model="gpt-5.2",
        input=problem.question_text + "\n" + problem.answer_text,
        output_schema=AnnotationSchema,  # Pydanticモデル
        instructions="""
        あなたは20年以上の経験を持つ塾講師です。
        以下の問題を分析し、指定されたフォーマットでアノテーションしてください。
        難易度は新潟県公立高校入試を基準にしてください。
        """
    )

    # Step 2: 既存の人間アノテーションとの一致率チェック
    if annotation.confidence < 0.85:
        annotation.verification_status = "needs_human_review"
    else:
        annotation.verification_status = "auto_verified"

    return annotation
```

#### Phase 3: LLMアノテーションの較正（Calibration）

```
目標: LLMの難易度判定を塾講師の判定と一致させる

1. 塾講師が100問をアノテーション（ゴールドスタンダード）
2. 同じ100問をLLMがアノテーション
3. 一致率を計算（Cohen's κ）
4. 不一致のケースを分析し、プロンプトを改善
5. 1-4を繰り返して κ ≥ 0.80 を目指す

長期的には:
- LLMアノテーション + LightGBM較正モデル（既存プロトタイプの calibrate.py）
- 実際の生徒の正答率データでIRT較正 → 客観的な難易度パラメータ
```

### 7.4 統一模試データの活用

統一模試協会から得られるデータ：
- 問題文・正答
- 問題ごとの正答率
- 受験者数

**活用方法：**

```
統一模試データ
  │
  ├─① IRT較正のアンカー（anchor items）
  │   大規模サンプル（数千〜数万人）の正答率から
  │   高精度なIRTパラメータを推定。
  │   モンジェネDBの他の問題の較正基準となる。
  │
  ├─② 難易度レベルの客観的基準
  │   正答率に基づく難易度分類:
  │   ・正答率 ≥90%: Level 1（基礎）
  │   ・70-89%: Level 2（標準）
  │   ・50-69%: Level 3（やや難）
  │   ・30-49%: Level 4（難）
  │   ・<30%: Level 5（発展）
  │   5%刻みで21段階の細分化も可能
  │
  ├─③ 出題傾向分析
  │   年度×単元のクロス集計 → 出題頻度マップ
  │   志望校ごとの重点単元を特定
  │
  └─④ LLM生成問題の品質ベンチマーク
      「統一模試Level 3の問題」と「LLM生成のLevel 3の問題」を
      同一生徒群に解かせて、正答率が近いかを検証
```

### 7.5 埋め込みベクトルの活用

既存プロトタイプ（gen-que v1）では、Gemini Embedding API (768次元) + ChromaDB でハイブリッド検索を実装済み。これを拡張する：

```
埋め込みベクトルの活用マップ

1. 問題-問題 類似度
   ・類似問題の検索（解き直し用の数値変更問題を探す）
   ・問題の重複検出（DBの品質管理）
   ・問題クラスタリング（同系統の問題をグループ化）

2. 問題-概念 マッチング
   ・概念タグの自動付与（概念の埋め込みと問題の埋め込みのcos類似度）
   ・知識グラフの自動構築補助

3. 生徒-問題 マッチング
   ・生徒の苦手プロファイルを埋め込みベクトル化
   ・「この生徒に最適な問題」をベクトル空間上の近傍探索で高速に発見
   ・既存の collaborative filtering 的アプローチ
     （似た苦手パターンの生徒に効果があった問題を推薦）

4. 誤答-矯正問題 マッチング
   ・誤答パターンの埋め込みと矯正用問題の埋め込みをマッチング
   ・「この間違い方をする生徒にはこの問題が効く」を学習

5. 解説スタイルの個別最適化
   ・同じ問題に対する複数の解説パターンを用意
   ・生徒の理解パターン（視覚的/手順的/概念的）に合った解説を選択
   ・前提知識が不足している生徒には、前提から説明する解説を選択
```

### 7.6 フライホイール効果（データの好循環）

```
                    ┌────────────────────┐
                    │ 問題DB拡充         │
                    │ ・LLM生成→検証     │
                    │ ・模試データ投入    │
                    │ ・アノテーション    │
                    └────────┬───────────┘
                             │
                     ┌───────▼────────┐
           ┌─────────│ 生徒が問題を解く│──────────┐
           │         └───────┬────────┘          │
           │                 │                    │
    ┌──────▼───────┐  ┌─────▼──────────┐  ┌─────▼──────────┐
    │ IRT較正精度↑  │  │ 苦手分析精度↑  │  │ 推薦精度↑      │
    │ 回答データ蓄積 │  │ エラーパターン │  │ Thompson       │
    │ → パラメータ   │  │ のデータ蓄積   │  │ Sampling の    │
    │   更新         │  │ → BKT精度向上  │  │ Beta分布更新   │
    └──────┬───────┘  └─────┬──────────┘  └─────┬──────────┘
           │                 │                    │
           └─────────────────┼────────────────────┘
                             │
                     ┌───────▼────────┐
                     │ 学習効果向上    │
                     │ → 口コミで      │
                     │   ユーザー増加  │
                     └───────┬────────┘
                             │
                     ┌───────▼────────┐
                     │ さらにデータ蓄積│
                     │ →さらに精度向上 │
                     └────────────────┘
```

**DreamBox は生徒1人あたり1時間で48,000データポイントを収集している。** モンジェネも同等以上のデータ密度を目指す：
- 回答の正誤
- 回答時間（ミリ秒単位）
- ヒント使用回数
- 解法ステップごとの時間配分
- 手書き答案からのエラーステップ位置
- 誤答パターンの自動分類結果

---

## 8. 技術アーキテクチャ

### 8.1 既存プロトタイプからの発展

```
現在（プロトタイプ）                     目標（提案システム）
──────────────────                     ──────────────────
[採点] 単発の手書き採点          →    蓄積型の苦手分析付き採点
[問題] 入力問題の類似3パターン   →    苦手プロファイルベースの個別生成
[DB]   ChromaDB + SQLite         →    Supabase (PostgreSQL + pgvector)
[分析] なし                      →    BKT + FSRS + IRT + 知識グラフ
[FB]   なし                      →    教師向けレポート + 生徒向けガイダンス
[UI]   採点結果表示のみ          →    マスタリー進捗ダッシュボード
```

### 8.2 システム構成図

```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend (Next.js)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ 問題表示 │ │ 答案撮影 │ │ 採点結果 │ │ マスタリー   │   │
│  │ (KaTeX)  │ │ /アップ  │ │ 表示     │ │ ダッシュボード│   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └──────┬───────┘   │
│       └────────────┴────────────┴───────────────┘           │
│                            │ SSE / REST                      │
└────────────────────────────┼─────────────────────────────────┘
                             │
┌────────────────────────────┼─────────────────────────────────┐
│                    Backend (FastAPI)                          │
│                            │                                  │
│  ┌─────────────────────────┼─────────────────────────────┐   │
│  │              API Layer  │                              │   │
│  │  ┌──────────┐ ┌────────┴───┐ ┌──────────┐           │   │
│  │  │ /grade   │ │ /generate  │ │ /student  │           │   │
│  │  │ /start   │ │ /problem   │ │ /profile  │           │   │
│  │  │ /stream  │ │            │ │ /report   │           │   │
│  │  └────┬─────┘ └─────┬──────┘ └────┬──────┘           │   │
│  └───────┼─────────────┼─────────────┼───────────────────┘   │
│          │             │             │                        │
│  ┌───────▼─────────────▼─────────────▼───────────────────┐   │
│  │                 Core Engine Layer                       │   │
│  │                                                        │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────────┐  │   │
│  │  │ Grading    │  │ Question   │  │ Adaptive       │  │   │
│  │  │ Agent      │  │ Generator  │  │ Engine         │  │   │
│  │  │ (GPT-5.2)  │  │ (LLM+DB)  │  │ (BKT/FSRS/IRT)│  │   │
│  │  └────────────┘  └────────────┘  └────────────────┘  │   │
│  │                                                        │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────────┐  │   │
│  │  │ Knowledge  │  │ Thompson   │  │ Teacher        │  │   │
│  │  │ Graph      │  │ Sampling   │  │ Feedback       │  │   │
│  │  │ Navigator  │  │ Selector   │  │ Generator      │  │   │
│  │  └────────────┘  └────────────┘  └────────────────┘  │   │
│  └────────────────────────────────────────────────────────┘   │
│                            │                                  │
└────────────────────────────┼──────────────────────────────────┘
                             │
┌────────────────────────────┼──────────────────────────────────┐
│              Supabase (PostgreSQL + pgvector)                  │
│                            │                                  │
│  ┌──────────┐ ┌───────────┴──┐ ┌────────────┐ ┌──────────┐  │
│  │ problems │ │ student_     │ │ knowledge_ │ │ student_ │  │
│  │ (問題DB) │ │ interactions │ │ graph_edges│ │ profiles │  │
│  │ +IRT     │ │ (回答ログ)   │ │ (知識グラフ)│ │ +BKT/FSRS│  │
│  │ +vector  │ │              │ │            │ │          │  │
│  └──────────┘ └──────────────┘ └────────────┘ └──────────┘  │
│                                                               │
│  ┌──────────────────┐  ┌─────────────────────────────────┐   │
│  │ misconception_   │  │ Storage: annotations bucket     │   │
│  │ patterns (誤答DB)│  │ (採点済み答案画像)               │   │
│  └──────────────────┘  └─────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────┘
```

### 8.3 競合との差別化ポイント

| 観点 | atama+ | Qubena | モンジェネ（提案） |
|-----|--------|--------|-------------------|
| **手書き答案対応** | ✗ タブレット入力のみ | △ 手書きメモ対応 | **◎ 手書き答案の完全AI採点** |
| **途中式の分析** | △ 結果のみ | △ 限定的 | **◎ ステップごとの誤り特定** |
| **問題生成** | ✗ 既存問題のみ | ✗ 既存問題のみ | **◎ AIによる無限生成** |
| **データベース** | ◎ 大規模 | ◎ 大規模 | △→◎ 統一模試+アノテーションで構築 |
| **塾講師連携** | ◎ コーチ役として | △ | **◎ 教師向けFBレポート** |
| **コスト** | 高い（月額制） | 高い（自治体向け） | **低い（API従量課金ベース）** |
| **紙との連携** | ✗ 完全デジタル | ✗ 完全タブレット | **◎ 紙の問題→撮影→AI採点** |

**モンジェネの最大の差別化：「紙とAIの融合」**
- 塾の現場では紙の問題用紙が依然として主流
- atama+やQubenaは完全タブレット化を前提としており、紙ベースの塾には導入障壁がある
- モンジェネは「既存の紙ワークフロー」に自然に溶け込む設計

---

## 9. 学術的根拠・引用一覧

### マスタリーラーニング
1. Bloom, B.S. (1984). "The 2 Sigma Problem: The Search for Methods of Group Instruction as Effective as One-to-One Tutoring." *Educational Researcher*, 13(6), 4-16.
2. Kulik, C.C., Kulik, J.A., & Bangert-Drowns, R.L. (1990). "Effectiveness of Mastery Learning Programs: A Meta-Analysis." *Review of Educational Research*, 60(2), 265-299. 効果量 d = 0.52（103研究）
3. Kulik, J.A. & Fletcher, J.D. (2016). "Effectiveness of Intelligent Tutoring Systems: A Meta-Analytic Review." *Review of Educational Research*, 86(1). 効果量 d = 0.66（中央値、50研究）
4. Nintil (2024). "Bloom's Two Sigma Problem: A Systematic Review." https://nintil.com/bloom-sigma/

### Knowledge Tracing
5. Corbett, A.T. & Anderson, J.R. (1994). "Knowledge Tracing: Modeling the Acquisition of Procedural Knowledge." *User Modeling and User-Adapted Interaction*, 4, 253-278.
6. Piech, C. et al. (2015). "Deep Knowledge Tracing." *NeurIPS*.
7. Zhang, J. et al. (2017). "Dynamic Key-Value Memory Networks for Knowledge Tracing." *WWW 2017*. arXiv:1611.08108

### Item Response Theory
8. Baker, F.B. & Kim, S.H. (2004). *Item Response Theory: Parameter Estimation Techniques*. CRC Press.
9. Shuai, Y. et al. (2024). "AutoIRT: Calibrating Item Response Theory Models with Automated Machine Learning." arXiv:2409.08823

### 間隔反復
10. Ye, J. et al. (2024). "FSRS: Free Spaced Repetition Scheduler." https://github.com/open-spaced-repetition/free-spaced-repetition-scheduler. SM-2比で20-30%の復習回数削減。

### 問題自動生成
11. Gierl, M.J. & Lai, H. (2013). "Automatic Item Generation." In *Handbook of Test Development*.
12. Qu, C. et al. (2024). "Automatic Question Generation for Educational Applications -- A Comparative Analysis Involving ChatGPT." *Computers and Education: Artificial Intelligence*.

### 強化学習と教育
13. IEEE HNICEM (2024). "Multi-Armed Bandit Algorithm as Reinforcement Learning Approach in Online Recommendation System of Learning Materials." Thompson Sampling: regret 26.8 vs epsilon-greedy 192.3

### 知識グラフ
14. Skycak, J. (2024). *The Math Academy Way: Using the Power of Science to Supercharge Math Learning*. ~2,500トピックの知識グラフ。
15. Doignon, J.P. & Falmagne, J.C. (1985). "Spaces for the assessment of knowledge." *International Journal of Man-Machine Studies*, 23, 175-196.

### エラー分類
16. Newman, M.A. (1977). "An Analysis of Sixth-Grade Pupils' Errors on Written Mathematical Tasks." *Research in Mathematics Education in Australia*.
17. Radatz, H. (1979). "Error Analysis in Mathematics Education." *Journal for Research in Mathematics Education*, 10(3), 163-172.

### 競合分析
18. atama plus (2024). 3,200以上の塾に導入。https://www.atama.plus/
19. COMPASS / Qubena (2024). 100万人以上のユーザー、170自治体、2,300以上の学校。中学数学を62時間→34時間に短縮。
20. DreamBox / Discovery Education. 1時間あたり48,000データポイント収集。

---

*本報告書は、既存プロトタイプのコード分析、学術論文調査、競合分析、Web検索に基づいて作成された。*
*作成日: 2026-02-27*
